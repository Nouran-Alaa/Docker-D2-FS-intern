PORT=3000
# If using local Mongo (from docker-compose)
MONGO_URI=mongodb://mongo:27017/usersdb

# Or if using MongoDB Atlas (replace <username>, <password>, <cluster>)
# MONGO_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/usersdb
